/*
 * 
 */
#ifndef __BASIC_H__
#define __BASIC_H__


/*
 * Method definitions.
 */

inline int is_char(char* );
inline int is_upalpha(char* );
inline int is_lopalpha(char* );
inline int is_alpha(char* );
inline int is_digit(char* );
inline int is_ctl(char* );
inline int is_cr(char* );
inline int is_lf(char* );
inline int is_sp(char* );
inline int is_ht(char* );
inline int is_double_quote(char* );


#endif /* __BASIC_H__ */
